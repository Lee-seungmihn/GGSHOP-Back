<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.ggshop.v1.mapper.MemberMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into tbl_member (member_email, member_password, member_name)
        values(#{memberEmail}, #{memberPassword}, #{memberName})
    </insert>

    <insert id="insertOauth">
        insert into tbl_member_oauth(id, provider)
        values (#{id}, #{provider})
    </insert>

    <!--  resultType 추가 -->
    <select id="selectMemberForLogin" resultType="com.app.ggshop.v1.domain.MemberVO">
        select id, member_email, member_password, member_name, member_status, created_date, updated_date
        from tbl_member
        where member_email = #{memberEmail} and member_password = #{memberPassword}
    </select>

    <!--  여기도 추가 -->
    <select id="selectByMemberEmail" resultType="com.app.ggshop.v1.domain.MemberVO">
        select id, member_email, member_password, member_name, member_status, created_date, updated_date
        from tbl_member
        where member_email = #{memberEmail}
    </select>

</mapper>