<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.ggshop.v1.mapper.profile.NicknameMapper">
    <update id="updateNickname">
        update tbl_member
        set member_nickname = #{memberNickname}
        where id = #{id}
    </update>
    <select id="selectNicknameById">
        select member_nickname from tbl_member where id = #{id}
    </select>
</mapper>